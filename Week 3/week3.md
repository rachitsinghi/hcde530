Attempt 01 - 
C2 — Code Literacy and Documentation
My proficiency in coding was illustrated in the ability to read, comprehend and modify several Python programs that were well organized in an intelligible manner to perform transformations, complete with relevant docstrings and inline comments describing the transformation logic and intentions. I have explained the assumptions underlying each step in the process, including handling issues such as dealing with null values and inconsistent role labels. This makes it easier for another person to comprehend the process from start to finish.

C3 — Data Cleaning and File Handling
Working heavily with unstructured CSVs, I created a pipeline that processes raw inputs from surveys to detect any data problems and correct them, ultimately generating structured output that is suitable for further analysis. The corrections performed by my pipeline include filtering out invalid rows (such as rows without a name), standardizing fields where different terms are used (such as labeling role), and fixing missing data to avoid runtime errors. With the pipeline working on structured input/output, I've been able to create an efficient process for data cleaning.

Attempt 02 -
C2 — Code Literacy and Documentation
I demonstrated code literacy by reading and restructuring my scripts to make both the logic and assumptions explicit for future use. I added docstrings to functions such as my word-count utilities to clearly define what inputs they take, what they return, and how they are used, and included inline comments explaining decisions like resolving file paths to ensure the script runs from any working directory. Through debugging, I also realized that documentation is most valuable when it supports reasoning, not just execution, so I focused on clarifying how and why transformations occur in the pipeline. In hindsight, I also recognize that my commit messages (e.g., “Fixed 2 bugs”) were too vague, and a stronger practice would be to explicitly document the problem and fix (such as identifying a ValueError caused by non-numeric data), reinforcing the role of documentation as a tool for traceability and understanding. 

C3 — Data Cleaning and File Handling 
I approached data cleaning as a diagnostic process rather than a mechanical one, using both runtime errors and incorrect outputs as signals to trace underlying issues in the dataset and logic. One key failure was a ValueError caused by non-numeric values (e.g., strings like “fifteen”) in the experience_years column, which revealed that it had assumed clean numeric input; I addressed this by identifying the offending rows and handling it by commading it to change verbal value to numeric value so the script could continue processing valid data. In addition to runtime errors, I also encountered a logical bug where the sorting step returned the lowest satisfaction scores instead of the highest, allowing me to examine not just whether the code executed, but whether the output matched the intended analysis. Fixing this required understanding how Python’s sorting behavior works and explicitly controlling the ordering logic. Through these iterations, I learned to treat both exceptions and “silent” logic errors as equally important diagnostic cues, and to build scripts that are resilient to messy, real-world data while producing consistent, trustworthy outputs.