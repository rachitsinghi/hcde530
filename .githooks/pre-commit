#!/bin/sh
# Refresh directory tree in .cursorrules before each commit.
set -e
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT" || exit 1
python3 scripts/refresh_cursorrules_tree.py
git add .cursorrules
